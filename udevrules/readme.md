# Adding udev rules for xbox controller and missile launcher
To add missile launcher and xbox control, copy `*.rules` in `/etc/udev/rules.d/` and restart udev with :
```bash
  sudo /etc/init.d/udev restart
```
